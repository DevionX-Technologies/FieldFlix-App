# FieldFlix Admin Web Configuration
# Leaving VITE_API_URL empty activates Vite's built-in backend proxy (bypasses browser CORS on localhost:5173).
# When deploying to production hosting (Vercel / Netlify / Render), set VITE_API_URL=https://fieldfflix-backend.onrender.com
VITE_API_URL=
